# Godot 生成的导入缓存与全局类索引，可完全重建
.godot/
*.translation

# 导出产物。export_presets.cfg 要进仓库，否则别人 clone 下来没法直接打包。
/build/
/export/
/ControllerTT.exe
/*.lnk

# 截图工具的输出，随时可用 scripts/shots.ps1 重新生成
/shots/

# 系统杂项
Thumbs.db
.DS_Store
